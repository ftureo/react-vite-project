// Lista que muestra todos los ItemProduct que existen 

export const ProductList = (props) => {
    console.log("las props en productList", props)
    return(
        <h1>Acá van a ir los productos que necesitemos llamando a ItemProduct</h1>
    )
}